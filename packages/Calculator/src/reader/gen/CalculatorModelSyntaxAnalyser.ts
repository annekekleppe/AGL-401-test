// Generated by the Freon Language Generator.
import * as AGL from "net.akehurst.language-agl-processor";
import { FreNamedNode, FreParseLocation, FreNodeReference } from "@freon4dsl/core";
import { CalculatorSyntaxAnalyserPart, CalculatorModelCommonSyntaxAnalyserPart } from ".";
import {Calculator} from "../../language/gen";

/**
 *   Class CalculatorModelSyntaxAnalyser is the main syntax analyser.
 *   The actual work is being done by its parts, one for each model unit,
 *   and one common part that contains the methods used in multiple units.
 *
 */
export class CalculatorModelSyntaxAnalyser implements AGL.SyntaxAnalyser<Calculator> {
    sourceName: string = "";
    locationMap: any;
    private _unit_Calculator_analyser: CalculatorSyntaxAnalyserPart = new CalculatorSyntaxAnalyserPart(this);
    private _unit_common_analyser: CalculatorModelCommonSyntaxAnalyserPart = new CalculatorModelCommonSyntaxAnalyserPart(this);

    clear(): void {
        throw new Error("Method not implemented.");
    }

    transform<T>(sppt: AGL.SharedPackedParseTree): T {
        if (!!sppt.root) {
            return this.transformSharedPackedParseTreeNode(sppt.root) as unknown as T;
        } else {
            return null;
        }
    }

    public transformSharedPackedParseTreeNode(node: AGL.SPPTNode): any {
        if (!!node) {
            try {
                if (node.isLeaf) {
                    return this.transformSharedPackedParseTreeLeaf(node);
                } else if (node.isBranch) {
                    return this.transformSharedPackedParseTreeBranch(node as AGL.SPPTBranch);
                }
            } catch (e) {
                if (e.message.startsWith("Syntax error in ") || e.message.startsWith("Error in CalculatorModelSyntaxAnalyser")) {
                    throw e;
                } else {
                    // add more info to the error message
                    throw new Error(
                        `Syntax error in "${this.sourceName} (line: ${node.location.line}, column: ${node.location.column})": ${e.message}`,
                    );
                }
                // console.log(e.message + e.stack);
            }
        } else {
            return null;
        }
    }

    private transformSharedPackedParseTreeLeaf(node: AGL.SPPTNode): any {
        let tmp = ((node as AGL.SPPTLeaf)?.nonSkipMatchedText).trim();
        if (tmp.length > 0) {
            if (tmp.startsWith('"')) {
                // stringLiteral, strip the surrounding quotes
                tmp = tmp.slice(1, tmp.length - 1);
                return tmp;
            } else if (tmp == "false") {
                // booleanLiteral
                return false;
            } else if (tmp == "true") {
                // booleanLiteral
                return true;
            } else if (Number.isInteger(parseInt(tmp, 10))) {
                // numberLiteral
                return parseInt(tmp, 10);
            } else {
                // identifier
                return tmp;
            }
        }
        return null;
    }

    private transformSharedPackedParseTreeBranch(branch: AGL.SPPTBranch): any {
        const brName: string = branch.name;
        if ("Calculator" === brName) {
            return this._unit_Calculator_analyser.transformCalculator(branch);
        } else if ("InputField" === brName) {
            return this._unit_Calculator_analyser.transformInputField(branch);
        } else if ("OutputField" === brName) {
            return this._unit_Calculator_analyser.transformOutputField(branch);
        } else if ("InputFieldReference" === brName) {
            return this._unit_Calculator_analyser.transformInputFieldReference(branch);
        } else if ("NumberLiteralExpression" === brName) {
            return this._unit_Calculator_analyser.transformNumberLiteralExpression(branch);
        } else if ("CalcExpression" === brName) {
            return this._unit_Calculator_analyser.transformCalcExpression(branch);
        } else if ("LiteralExpression" === brName) {
            return this._unit_Calculator_analyser.transformLiteralExpression(branch);
        } else if ("__fre_binary_CalcExpression" === brName) {
            return this._unit_Calculator_analyser.transform__fre_binary_CalcExpression(branch);
        } else if ("__fre_reference" === brName) {
            return this.transform__fre_reference(branch);
        } else {
            throw new Error(`Error in CalculatorModelSyntaxAnalyser: ${brName} not handled for node '${branch?.matchedText}'`);
        }
    }

    /**
     * Generic method to get the children of a branch. Throws an error if no children can be found.
     */
    public getChildren(branch: AGL.SPPTBranch): any {
        if (!!branch && !!branch.nonSkipChildren) {
            try {
                return branch.nonSkipChildren.toArray();
            } catch (e) {
                throw new Error(`Cannot follow branch: ${e.message} (${branch.matchedText.trimEnd()})`);
            }
        }
        return null;
    }

    /**
     * Generic method to get the optional group of a branch. Throws an error if no group can be found.
     */
    public getGroup(branch: AGL.SPPTBranch) {
        // take the first element in the [0..1] optional group or multi branch
        let group: any = branch;
        let stop: boolean = false;
        while (!stop) {
            let nextOne: any = null;
            try {
                nextOne = group.nonSkipChildren?.toArray()[0];
            } catch (e) {
                throw new Error(`Cannot follow group: ${e.message} (${group.matchedText})`);
            }
            if (!nextOne || (!nextOne.name.includes("multi") && !nextOne.name.includes("group"))) {
                stop = true; // found a branch with actual content, return its parent!
            } else {
                group = nextOne;
            }
        }
        return group;
    }

    public transform__fre_reference(branch: AGL.SPPTBranch) {
        if (branch.name.includes("multi") || branch.name.includes("List")) {
            // its a path name
            return this.transformSharedPackedParseTreeList<string>(branch, ".");
        } else {
            // its a single name
            return this.transformSharedPackedParseTreeLeaf(branch);
        }
    }

    /**
     * Generic method to transform references
     * ...FreNodeRef = identifier;
     */
    public freNodeRef<T extends FreNamedNode>(branch: AGL.SPPTBranch, typeName: string): FreNodeReference<T> {
        let referred: string | string[] | T = this.transformSharedPackedParseTreeNode(branch);
        if (this.getChildren(branch)?.length > 1) {
            // its a path name
            referred = this.transformSharedPackedParseTreeList<string>(branch, ".");
        }
        if (referred === null || referred === undefined) {
            // throw new Error(`Syntax error in "${branch?.parent?.matchedText}": cannot create empty reference`);
            return null;
        } else if (typeof referred === "string" && (referred as string).length === 0) {
            // throw new Error(`Syntax error in "${branch?.parent?.matchedText}": cannot create empty reference`);
            return null;
        } else {
            return FreNodeReference.create<T>(referred, typeName);
        }
    }

    /**
     * Generic method to transform lists
     */
    public transformSharedPackedParseTreeList<T>(branch: AGL.SPPTBranch, separator?: string): T[] {
        let result: T[] = [];
        const children = this.getChildren(branch);
        if (!!children) {
            for (const child of children) {
                let element: any = this.transformSharedPackedParseTreeNode(child);
                if (element !== null && element !== undefined) {
                    if (separator === null || separator === undefined) {
                        result.push(element);
                    } else {
                        if (element !== separator) {
                            result.push(element);
                        }
                    }
                }
            }
        }
        return result;
    }

    /**
     * Generic method to transform lists of references
     */
    public transformSharedPackedParseTreeRefList<T extends FreNamedNode>(
        branch: AGL.SPPTBranch,
        typeName: string,
        separator?: string,
    ): FreNodeReference<T>[] {
        let result: FreNodeReference<T>[] = [];
        const children = this.getChildren(branch);
        if (!!children) {
            for (const child of children) {
                let refName: any = this.transformSharedPackedParseTreeNode(child);
                if (refName !== null && refName !== undefined) {
                    if (separator === null || separator === undefined) {
                        result.push(FreNodeReference.create<T>(refName, typeName));
                    } else {
                        if (refName !== separator) {
                            result.push(FreNodeReference.create<T>(refName, typeName));
                        }
                    }
                }
            }
        }
        return result;
    }

    public location(branch: AGL.SPPTBranch): FreParseLocation {
        return FreParseLocation.create({
            filename: this.sourceName,
            line: branch.location.line,
            column: branch.location.column,
        });
    }

    // the rest is added to adhere to the interface
    readonly __doNotUseOrImplementIt;
    readonly embeddedSyntaxAnalyser: any;

    configure(configurationContext: AGL.SentenceContext<AGL.GrammarItem>, configuration?: any): any {
    }
}
