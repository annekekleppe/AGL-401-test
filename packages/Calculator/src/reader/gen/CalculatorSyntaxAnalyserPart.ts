// Generated by the Freon Language Generator.
import {
    type KtList,
    type Sentence,
    type SpptDataNodeInfo,
} from "net.akehurst.language-agl-processor/net.akehurst.language-agl-processor.mjs";
import { FreNodeReference } from "@freon4dsl/core";
import {
    Calculator,
    InputField,
    OutputField,
    InputFieldReference,
    NumberLiteralExpression,
    CalcExpression,
    LiteralExpression,
    MultiplyExpression,
    PlusExpression,
    MinusExpression,
    DivideExpression,
} from "../../language/gen/index.js";
import { PrimValueType, CalculatorModelSyntaxAnalyser } from "./CalculatorModelSyntaxAnalyser.js";

export class CalculatorSyntaxAnalyserPart {
    mainAnalyser: CalculatorModelSyntaxAnalyser;

    constructor(mainAnalyser: CalculatorModelSyntaxAnalyser) {
        this.mainAnalyser = mainAnalyser;
    }

    /**
     * Method to transform branches that match the following rule:
     * Calculator = 'Calculator' identifier
     *	 InputField*
     *	 OutputField* ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformCalculator(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): Calculator {
        // console.log('4 transformCalculator called: ' + children.toString());
        let __name: string;
        let __inputFields: InputField[];
        let __outputFields: OutputField[];
        __name = this.mainAnalyser.transformPrimValue<string>(children.asJsReadonlyArrayView()[1], PrimValueType.identifier); // RHSPrimEntry
        __inputFields = children.asJsReadonlyArrayView()[2].asJsReadonlyArrayView(); // RHSPartListEntry
        __outputFields = children.asJsReadonlyArrayView()[3].asJsReadonlyArrayView(); // RHSPartListEntry

        return Calculator.create({
            name: __name,
            inputFields: __inputFields,
            outputFields: __outputFields,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * InputField = 'input' identifier ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformInputField(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): InputField {
        // console.log('4 transformInputField called: ' + children.toString());
        let __name: string;
        __name = this.mainAnalyser.transformPrimValue<string>(children.asJsReadonlyArrayView()[1], PrimValueType.identifier); // RHSPrimEntry

        return InputField.create({
            name: __name,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * OutputField = 'output' CalcExpression ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformOutputField(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): OutputField {
        // console.log('4 transformOutputField called: ' + children.toString());
        let __expression: CalcExpression;
        __expression = children.asJsReadonlyArrayView()[1]; // RHSPartEntry

        return OutputField.create({
            expression: __expression,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * InputFieldReference = __fre_reference ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformInputFieldReference(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): InputFieldReference {
        // console.log('4 transformInputFieldReference called: ' + children.toString());
        let __field: FreNodeReference<InputField>;
        __field = this.mainAnalyser.makeFreNodeRef<InputField>(children.asJsReadonlyArrayView()[0], "InputField"); // RHSRefEntry

        return InputFieldReference.create({
            field: __field,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * NumberLiteralExpression = stringLiteral ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformNumberLiteralExpression(
        nodeInfo: SpptDataNodeInfo,
        children: KtList<object>,
        sentence: Sentence,
    ): NumberLiteralExpression {
        // console.log('4 transformNumberLiteralExpression called: ' + children.toString());
        let __value: string;
        __value = this.mainAnalyser.transformPrimValue<string>(children.asJsReadonlyArrayView()[0], PrimValueType.string); // RHSPrimEntry

        return NumberLiteralExpression.create({
            value: __value,
            parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
        });
    }

    /**
     * Method to transform branches that match the following rule:
     * CalcExpression = InputFieldReference
     *    | LiteralExpression
     *    | __fre_binary_CalcExpression ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformCalcExpression(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): CalcExpression {
        // console.log('3 transformCalcExpression called: ' + children.toString());
        return children.asJsReadonlyArrayView()[0];
    }

    /**
     * Method to transform branches that match the following rule:
     * LiteralExpression = NumberLiteralExpression  ;
     * @param nodeInfo
     * @param children
     * @param sentence
     */
    public transformLiteralExpression(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): LiteralExpression {
        // console.log('3 transformLiteralExpression called: ' + children.toString());
        return children.asJsReadonlyArrayView()[0];
    }

    /**
     * Generic method to transform binary expressions, which are parsed
     * according to these rules:
     * __fre_binary_CalcExpression = [CalcExpression / __fre_binary_operator]2+ ;
     * leaf __fre_binary_operator = '*' | '+' | '-' | '/' ;
     *
     * In this method we build a crooked tree, which in a later phase needs to be balanced
     * according to the priorities of the operators.
     * @param branch
     * @private
     */
    public transform__fre_binary_CalcExpression(nodeInfo: SpptDataNodeInfo, children: KtList<object>, sentence: Sentence): CalcExpression {
        // console.log('transform__fre_binary_CalcExpression called: ' + children.toString());
        let index = 0;
        let list = children.asJsReadonlyArrayView();
        let first = list[index++];
        while (index < list.length) {
            let operator = list[index++];
            let second = list[index++];
            let combined: CalcExpression = null;
            switch (operator) {
                case "*": {
                    combined = MultiplyExpression.create({
                        left: first,
                        right: second,
                        parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
                    });
                    break;
                }
                case "+": {
                    combined = PlusExpression.create({
                        left: first,
                        right: second,
                        parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
                    });
                    break;
                }
                case "-": {
                    combined = MinusExpression.create({
                        left: first,
                        right: second,
                        parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
                    });
                    break;
                }
                case "/": {
                    combined = DivideExpression.create({
                        left: first,
                        right: second,
                        parseLocation: this.mainAnalyser.location(sentence, nodeInfo.node),
                    });
                    break;
                }
                default: {
                    combined = null;
                }
            }
            first = combined;
        }
        return first;
    }
}
