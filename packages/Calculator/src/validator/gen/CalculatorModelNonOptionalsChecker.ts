// Generated by the Freon Language Generator.
// TEMPLATE: NonOptionalsCheckerTemplate.generateChecker(...)

import { FreError, FreErrorSeverity, type FreWriter, FreLanguageEnvironment } from "@freon4dsl/core";
import {
    CalculatorModel,
    Calculator,
    InputField,
    OutputField,
    InputFieldReference,
    NumberLiteralExpression,
    BinaryExpression,
    MultiplyExpression,
    PlusExpression,
    MinusExpression,
    DivideExpression,
} from "../../language/gen/index.js";

import { CalculatorModelDefaultWorker } from "../../utils/gen/index.js";
import { type CalculatorModelCheckerInterface } from "./CalculatorModelValidator.js";

/**
 * Class CalculatorModelNonOptionalsChecker is part of the implementation of the default validator.
 * It checks whether non-optional properties, as such defined in the .ast definition, indeed
 * have a value.
 * Class CalculatorModelWalker implements the traversal of the model tree. This class implements
 * the actual checking of each node in the tree.
 */
export class CalculatorModelNonOptionalsChecker extends CalculatorModelDefaultWorker implements CalculatorModelCheckerInterface {
    // 'myWriter' is used to provide error messages on the nodes in the model tree
    myWriter: FreWriter = FreLanguageEnvironment.getInstance().writer;
    // 'errorList' holds the errors found while traversing the model tree
    errorList: FreError[] = [];

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeCalculatorModel(node: CalculatorModel): boolean {
        if (node.name === null || node.name === undefined || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }
        if (node.calc === null || node.calc === undefined) {
            this.errorList.push(new FreError("Property 'calc' must have a value", node, node.name, "calc", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeCalculator(node: Calculator): boolean {
        if (node.name === null || node.name === undefined || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeInputField(node: InputField): boolean {
        if (node.name === null || node.name === undefined || node.name?.length === 0) {
            this.errorList.push(new FreError("Property 'name' must have a value", node, node.name, "name", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeOutputField(node: OutputField): boolean {
        if (node.expression === null || node.expression === undefined) {
            this.errorList.push(
                new FreError("Property 'expression' must have a value", node, "unnamed", "expression", FreErrorSeverity.Error),
            );
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeInputFieldReference(node: InputFieldReference): boolean {
        if (node.field === null || node.field === undefined) {
            this.errorList.push(new FreError("Property 'field' must have a value", node, "unnamed", "field", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeNumberLiteralExpression(node: NumberLiteralExpression): boolean {
        if (node.value === null || node.value === undefined || node.value?.length === 0) {
            this.errorList.push(new FreError("Property 'value' must have a value", node, "unnamed", "value", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeBinaryExpression(node: BinaryExpression): boolean {
        if (node.left === null || node.left === undefined) {
            this.errorList.push(new FreError("Property 'left' must have a value", node, "unnamed", "left", FreErrorSeverity.Error));
        }
        if (node.right === null || node.right === undefined) {
            this.errorList.push(new FreError("Property 'right' must have a value", node, "unnamed", "right", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeMultiplyExpression(node: MultiplyExpression): boolean {
        if (node.left === null || node.left === undefined) {
            this.errorList.push(new FreError("Property 'left' must have a value", node, "unnamed", "left", FreErrorSeverity.Error));
        }
        if (node.right === null || node.right === undefined) {
            this.errorList.push(new FreError("Property 'right' must have a value", node, "unnamed", "right", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforePlusExpression(node: PlusExpression): boolean {
        if (node.left === null || node.left === undefined) {
            this.errorList.push(new FreError("Property 'left' must have a value", node, "unnamed", "left", FreErrorSeverity.Error));
        }
        if (node.right === null || node.right === undefined) {
            this.errorList.push(new FreError("Property 'right' must have a value", node, "unnamed", "right", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeMinusExpression(node: MinusExpression): boolean {
        if (node.left === null || node.left === undefined) {
            this.errorList.push(new FreError("Property 'left' must have a value", node, "unnamed", "left", FreErrorSeverity.Error));
        }
        if (node.right === null || node.right === undefined) {
            this.errorList.push(new FreError("Property 'right' must have a value", node, "unnamed", "right", FreErrorSeverity.Error));
        }

        return false;
    }

    /**
     * Checks 'node' before checking its children.
     * Found errors are pushed onto 'errorlist'.
     * If an error is found, it is considered 'fatal', which means that no other checks on
     * 'node' are performed.
     *
     * @param node
     */
    public execBeforeDivideExpression(node: DivideExpression): boolean {
        if (node.left === null || node.left === undefined) {
            this.errorList.push(new FreError("Property 'left' must have a value", node, "unnamed", "left", FreErrorSeverity.Error));
        }
        if (node.right === null || node.right === undefined) {
            this.errorList.push(new FreError("Property 'right' must have a value", node, "unnamed", "right", FreErrorSeverity.Error));
        }

        return false;
    }
}
